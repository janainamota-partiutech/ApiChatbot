spring.application.name=ApiChatbot
# Configura��o do PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/Api_chatbot
spring.datasource.username=postgres
spring.datasource.password=12344

# Configura��o do JPA para gerar tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configura��es do log para mostrar informa��es da conex�o com o banco
logging.level.org.springframework.jdbc=DEBUG 
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicTypeDescriptor=TRACE